@echo off
REM Get the current directory where this script is running
set CURRENT_DIR=%~dp0

REM Launch Java with the relative paths
REM We assume the 'javafx-sdk' folder is inside this same directory
java --module-path "%CURRENT_DIR%javafx-sdk-25.0.1\lib" --add-modules javafx.controls,javafx.fxml -jar "%CURRENT_DIR%backgammonJarTry1.jar"

pause